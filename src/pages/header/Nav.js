import React from 'react';
import { styled } from 'styled-components';

const Nav = () => {
    return (
        <NavigationGrid>
            <H2>반응형 사이트 전체 메뉴</H2>
        </NavigationGrid>
    );
};

export default Nav;

const NavigationGrid = styled.nav`

`

const H2 = styled.h2`

`